version: 2
distro_name: starter
apis:
- batches
- file_processors
- files
- inference
- interactions
- messages
- responses
- skills
- tool_runtime
- vector_io
providers:
  inference:
    - provider_id: openai
      provider_type: remote::openai
      config:
        api_key: __DEEPSEEK_API_KEY__
        base_url: https://api.deepseek.com/v1
        network:
          tls:
            verify: true
    - provider_id: opencode-go
      provider_type: remote::openai
      config:
        api_key: ${env.OPENCODE_GO_API_KEY:=}
        base_url: https://opencode.ai/zen/go/v1
        network:
          tls:
            verify: true
    - provider_id: sentence-transformers
      provider_type: inline::sentence-transformers
      config:
        trust_remote_code: true
  vector_io:
  - provider_id: faiss
    provider_type: inline::faiss
    config:
      persistence:
        namespace: vector_io::faiss
        backend: kv_default
  - provider_id: sqlite-vec
    provider_type: inline::sqlite-vec
    config:
      db_path: ${env.SQLITE_STORE_DIR:=~/.ogx/distributions/starter}/sqlite_vec.db
      persistence:
        namespace: vector_io::sqlite_vec
        backend: kv_default
  - provider_id: ${env.MILVUS_URL:+milvus}
    provider_type: inline::milvus
    config:
      db_path: ${env.MILVUS_DB_PATH:=~/.ogx/distributions/starter}/milvus.db
      persistence:
        namespace: vector_io::milvus
        backend: kv_default
  - provider_id: ${env.CHROMADB_URL:+chromadb}
    provider_type: remote::chromadb
    config:
      url: ${env.CHROMADB_URL:=}
      persistence:
        namespace: vector_io::chroma_remote
        backend: kv_default
  - provider_id: ${env.PGVECTOR_DB:+pgvector}
    provider_type: remote::pgvector
    config:
      host: ${env.PGVECTOR_HOST:=localhost}
      port: ${env.PGVECTOR_PORT:=5432}
      db: ${env.PGVECTOR_DB:=}
      user: ${env.PGVECTOR_USER:=}
      password: ${env.PGVECTOR_PASSWORD:=}
      distance_metric: COSINE
      vector_index:
        type: HNSW
        m: 16
        ef_construction: 64
        ef_search: 40
      persistence:
        namespace: vector_io::pgvector
        backend: kv_default
  - provider_id: ${env.QDRANT_URL:+qdrant}
    provider_type: remote::qdrant
    config:
      api_key: ${env.QDRANT_API_KEY:=}
      persistence:
        namespace: vector_io::qdrant_remote
        backend: kv_default
  - provider_id: ${env.WEAVIATE_CLUSTER_URL:+weaviate}
    provider_type: remote::weaviate
    config:
      weaviate_api_key: null
      weaviate_cluster_url: ${env.WEAVIATE_CLUSTER_URL:=localhost:8080}
      persistence:
        namespace: vector_io::weaviate
        backend: kv_default
  - provider_id: ${env.ELASTICSEARCH_URL:+elasticsearch}
    provider_type: remote::elasticsearch
    config:
      elasticsearch_url: ${env.ELASTICSEARCH_URL:=localhost:9200}
      elasticsearch_api_key: ${env.ELASTICSEARCH_API_KEY:=}
      persistence:
        namespace: vector_io::elasticsearch
        backend: kv_default
  - provider_id: ${env.INFINISPAN_URL:+infinispan}
    provider_type: remote::infinispan
    config:
      url: ${env.INFINISPAN_URL:=http://localhost:11222}
      username: ${env.INFINISPAN_USERNAME:=admin}
      password: ${env.INFINISPAN_PASSWORD:=}
      use_https: false
      auth_mechanism: digest
      verify_tls: true
      persistence:
        namespace: vector_io::infinispan
        backend: kv_default
  files:
  - provider_id: builtin-files
    provider_type: inline::localfs
    config:
      storage_dir: ${env.FILES_STORAGE_DIR:=~/.ogx/distributions/starter/files}
      metadata_store:
        table_name: files_metadata
        backend: sql_default
  file_processors:
  - provider_id: auto
    provider_type: inline::auto
  interactions:
  - provider_id: builtin
    provider_type: inline::builtin
    config:
      store:
        table_name: interactions
        backend: sql_default
  messages:
  - provider_id: builtin
    provider_type: inline::builtin
    config:
      kvstore:
        namespace: message_batches
        backend: kv_default
  responses:
  - provider_id: builtin
    provider_type: inline::builtin
    config:
      persistence:
        responses:
          table_name: responses
          backend: sql_default
          max_write_queue_size: 10000
          num_writers: 4
      memory_config:
        default_vector_store_provider_id: sqlite-vec
      # Background retention worker: deletes responses older than 30
      # days so the durable SQL store does not grow without bound.
      # Mirrors OpenAI's Responses API contract. Incremental
      # children are materialized to self-contained snapshots
      # before their parent is removed, so chains stay consistent
      # for survivors. Set `enabled: false` to disable.
      retention:
        enabled: true
        retention_seconds: 2592000   # 30 days
        sweep_interval_seconds: 3600  # 1 hour
        batch_size: 100
  skills:
  - provider_id: builtin
    provider_type: inline::builtin
    config:
      persistence:
        namespace: skills
        backend: kv_default
  tool_runtime:
  - provider_id: brave-search
    provider_type: remote::brave-search
    config:
      api_key: ${env.BRAVE_SEARCH_API_KEY:=}
      max_results: 3
  - provider_id: tavily-search
    provider_type: remote::tavily-search
    config:
      api_key: ${env.TAVILY_SEARCH_API_KEY:=}
      max_results: 3
  - provider_id: nimble-search
    provider_type: remote::nimble-search
    config:
      api_key: ${env.NIMBLE_API_KEY:=}
      max_results: 3
      search_depth: lite
  - provider_id: file-search
    provider_type: inline::file-search
  - provider_id: model-context-protocol
    provider_type: remote::model-context-protocol
  batches:
  - provider_id: reference
    provider_type: inline::reference
    config:
      sqlstore:
        table_name: batches
        backend: sql_default
storage:
  backends:
    kv_default:
      type: kv_sqlite
      db_path: /data/ogx_kvstore.db
    sql_default:
      type: sql_sqlite
      db_path: /data/ogx_sqlstore.db
  stores:
    metadata:
      namespace: registry
      backend: kv_default
    inference:
      table_name: inference_store
      backend: sql_default
      max_write_queue_size: 10000
      num_writers: 4
    conversations:
      table_name: openai_conversations
      backend: sql_default
    prompts:
      table_name: prompts
      backend: sql_default
    connectors:
      table_name: connectors
      backend: sql_default
registered_resources:
  models:
    # Static fallback used only when the gateway model list can't be fetched at
    # boot (see entrypoint.sh). Keep this in sync with the admin dashboard;
    # the runtime gateway sync reconciles to the dashboard list every few
    # minutes and adds/removes models from the TiDB registry.
    - metadata: {_unprefixed_alias: true}
      model_id: deepseek-v4-flash
      provider_id: openai
      provider_model_id: deepseek-v4-flash
      model_type: llm
    - metadata: {_unprefixed_alias: true}
      model_id: muse-spark-1.2
      provider_id: opencode-go
      provider_model_id: muse-spark-1.2
      model_type: llm
    - metadata: {_unprefixed_alias: true}
      model_id: mimo-v2.5
      provider_id: opencode-go
      provider_model_id: mimo-v2.5
      model_type: llm
    - metadata: {_unprefixed_alias: true}
      model_id: ox-alpha-free
      provider_id: opencode-go
      provider_model_id: ox-alpha-free
      model_type: llm
    - metadata: {_unprefixed_alias: true}
      model_id: hy3
      provider_id: opencode-go
      provider_model_id: hy3
      model_type: llm
  vector_stores: []
server:
  port: 8321
vector_stores:
  default_provider_id: faiss
  default_embedding_model:
    provider_id: sentence-transformers
    model_id: nomic-ai/nomic-embed-text-v1.5
  default_reranker_model:
    provider_id: sentence-transformers
    model_id: Qwen/Qwen3-Reranker-0.6B
  file_search_params:
    header_template: 'file_search tool found {num_chunks} chunks:

      BEGIN of file_search tool results.

      '
    footer_template: 'END of file_search tool results.

      '
  context_prompt_params:
    chunk_annotation_template: 'Result {index}

      Content: {chunk.content}

      Metadata: {metadata}

      '
    context_template: 'The above results were retrieved to help answer the user''s
      query: "{query}". Use them as supporting information only in answering this
      query. {annotation_instruction}

      '
  annotation_prompt_params:
    enable_annotations: true
    annotation_instruction_template: Cite sources immediately at the end of sentences
      before punctuation, using `<|file-id|>` format like 'This is a fact <|file-Cn3MSNn72ENTiiq11Qda4A|>.'.
      Do not add extra punctuation. Use only the file IDs provided, do not invent
      new ones.
    chunk_annotation_template: '[{index}] {metadata_text} cite as <|{file_id}|>

      {chunk_text}

      '
  file_ingestion_params:
    default_chunk_size_tokens: 512
    default_chunk_overlap_tokens: 128
  chunk_retrieval_params:
    chunk_multiplier: 5
    max_tokens_in_context: 4000
    default_reranker_strategy: rrf
    rrf_impact_factor: 60.0
    weighted_search_alpha: 0.5
    default_search_mode: vector
  file_batch_params:
    max_concurrent_files_per_batch: 3
    file_batch_chunk_size: 10
    cleanup_interval_seconds: 86400
  contextual_retrieval_params:
    default_timeout_seconds: 120
    default_max_concurrency: 3
    max_document_tokens: 100000
connectors: []
